# 明日北极星 - Polaris of Tomorrow

一个优雅的个人分享网站，包含实用工具集合。

## ✨ 特性

- 🌍 **多语言支持**：简体中文、繁体中文、英语
- 🎨 **极光主题设计**：独特的北极光视觉风格
- 🛠️ **实用工具**：
  - ⚖️ 单位换算器
  - 📅 日历转换（农历/阳历）
  - ✈️ 行程记录
- 📱 **响应式设计**：完美支持桌面和移动设备
- ⚡ **流畅动画**：基于 Framer Motion 的精美动画效果

## 🚀 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **动画**: Framer Motion
- **国际化**: next-intl
- **包管理**: pnpm

## 📦 安装

```bash
# 安装 pnpm（如果还没有安装）
npm install -g pnpm

# 安装依赖
pnpm install
```

## 🏃‍♂️ 运行

```bash
# 开发模式
pnpm dev

# 构建生产版本
pnpm build

# 运行生产版本
pnpm start
```

访问 `http://localhost:3000` 查看网站。

## 📁 项目结构

```
polaris-tools/
├── src/
│   ├── app/
│   │   ├── [locale]/          # 多语言路由
│   │   │   ├── page.tsx       # 首页
│   │   │   ├── about/         # 关于页面
│   │   │   └── tools/         # 工具页面
│   │   │       ├── unit-converter/
│   │   │       ├── calendar-converter/
│   │   │       └── journey-tracker/
│   │   └── globals.css        # 全局样式
│   ├── components/            # React 组件
│   ├── lib/                   # 工具函数
│   └── i18n.ts               # 国际化配置
├── messages/                  # 语言文件
│   ├── zh-CN.json
│   ├── zh-TW.json
│   └── en.json
└── public/                    # 静态资源
    └── videos/               # 极光视频
```

## 🎨 自定义

### 添加极光视频

1. 将您的极光视频文件重命名为 `aurora.mp4`
2. 放置在 `public/videos/` 目录下
3. 在 `src/app/[locale]/about/page.tsx` 中取消注释视频标签

### 修改主题颜色

编辑 `tailwind.config.ts` 中的颜色配置：

```typescript
colors: {
  aurora: { ... },  // 极光色系
  polar: { ... },   // 北极色系
  night: { ... },   // 夜空色系
}
```

## 🌐 多语言

支持的语言：
- 简体中文 (`zh-CN`)
- 繁体中文 (`zh-TW`)
- 英语 (`en`)

默认语言为简体中文。

## 📝 许可

MIT License

## 👤 作者

常居北欧，热爱极光和技术

---

**Polaris of Tomorrow** - 明日北极星
